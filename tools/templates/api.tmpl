package {{.PkgName}}_{{.ApiGroupName}}

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	{{if .NeedStrconv}}
	    "strconv"
	{{end}}

	{{if .ProcessChineseData}}
	    "strings"
    {{end}}

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"

{{if .HasURIBinding}}
	"github.com/jtacoma/uritemplates"
{{end}}
)

{{range .Apis}}
// ==============================================================================================
// API: {{.Name}}
// ==============================================================================================

type {{.Name}}Fields struct {
{{range .Params}}	{{if .IsBindingUri}}{{.Name}} {{.Type}} `uri:"{{.ParamName}}" binding:"required"` // {{.Description}}
	{{else}}{{.Name}} {{.Type}} `form:"{{.ParamName}}{{if .DefaultValue}},default={{.DefaultValue}}{{end}}"` // {{.Description}}
{{end}}{{end}}
	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHook{{.Name}} allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHook{{.Name}} func(context.Context, *{{.Name}}Fields) (any, error)

// String{{.Name}} executes the request and returns the raw response body string.
func String{{.Name}}(ctx context.Context, fields *{{.Name}}Fields) (string, error) {
	if fields == nil {
		fields = &{{.Name}}Fields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("{{.Name}}")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "{{.Method}}",
			Path:     "{{.Path}}",
			CnRegion: {{.CnRegion}},
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	{{range .Params}}{{if .DefaultValue}}
	{{if eq .Type "string"}}if fields.{{.Name}} == "" {
		fields.{{.Name}} = "{{.DefaultValue}}"
	}
	{{else if eq .Type "[]int"}}
    /* if fields.{{.Name}} == 0 {
       		fields.{{.Name}} = {{.DefaultValue}}
       	} */
	{{else}}if fields.{{.Name}} == 0 {
		fields.{{.Name}} = {{.DefaultValue}}
	}{{end}}
	{{end}}{{end}}

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	{{if .HasURIBinding}}
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		{{range .Params}}{{if .IsBindingUri}}"{{.ParamName}}": fields.{{.Name}},
    		{{end}}{{end}}
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	{{else}}
    	req.URL.Path = fields.Path
    	{{end}}
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	{{range .Params}}{{if not .IsBindingUri}}
    {{if eq .Type "int"}}
    	if !q.Has("{{.ParamName}}") {
    		q.Add("{{.ParamName}}", strconv.Itoa(fields.{{.Name}}))
    	}
    {{else if eq .Type "[]int"}} // q.Add("{{.ParamName}}", fields.{{.Name}})
    {{else}}
	if !q.Has("{{.ParamName}}") {
		q.Add("{{.ParamName}}", "{{.DefaultValue}}")
	}
    {{end}}{{end}}{{end}}


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridge{{.Name}} routes the request to either CN or Global logic based on input.
func bridge{{.Name}}(ctx context.Context, fields *{{.Name}}Fields) (any, error) {
    {{if .ProcessChineseData}}
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				{{if .IsGuild}}
				Name:      fields.NameSlug,
				{{else}}
				Name:      fields.CharacterName,
				{{end}}
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	{{end}}

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHook{{.Name}} != nil {
			return CNHook{{.Name}}(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := String{{.Name}}(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &{{.Name}}Model{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// {{.Name}}
/* {{.Description}} */
// Path: {{.Path}}
var {{.Name}} = bridge{{.Name}}

{{end}}