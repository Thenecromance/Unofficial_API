package D3_D3ArtisanandRecipe

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: GetArtisan
// ==============================================================================================

type GetArtisanFields struct {
	ArtisanSlug string `uri:"artisanSlug" binding:"required"` // The slug of the artisan to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetArtisan allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetArtisan func(context.Context, *GetArtisanFields) (any, error)

// StringGetArtisan executes the request and returns the raw response body string.
func StringGetArtisan(ctx context.Context, fields *GetArtisanFields) (string, error) {
	if fields == nil {
		fields = &GetArtisanFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetArtisan")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/data/artisan/{artisanSlug}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.ArtisanSlug == "" {
		fields.ArtisanSlug = "blacksmith"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"artisanSlug": fields.ArtisanSlug,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetArtisan routes the request to either CN or Global logic based on input.
func bridgeGetArtisan(ctx context.Context, fields *GetArtisanFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetArtisan != nil {
			return CNHookGetArtisan(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetArtisan(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetArtisanModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetArtisan
/* GetArtisan Returns a single artisan by slug. */
// Path: /d3/data/artisan/{artisanSlug}
var GetArtisan = bridgeGetArtisan


// ==============================================================================================
// API: GetRecipe
// ==============================================================================================

type GetRecipeFields struct {
	ArtisanSlug string `uri:"artisanSlug" binding:"required"` // The slug of the artisan to retrieve.
		RecipeSlug string `uri:"recipeSlug" binding:"required"` // The slug of the recipe to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetRecipe allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetRecipe func(context.Context, *GetRecipeFields) (any, error)

// StringGetRecipe executes the request and returns the raw response body string.
func StringGetRecipe(ctx context.Context, fields *GetRecipeFields) (string, error) {
	if fields == nil {
		fields = &GetRecipeFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetRecipe")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/data/artisan/{artisanSlug}/recipe/{recipeSlug}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.ArtisanSlug == "" {
		fields.ArtisanSlug = "blacksmith"
	}
	
	
	if fields.RecipeSlug == "" {
		fields.RecipeSlug = "apprentice-flamberge"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"artisanSlug": fields.ArtisanSlug,
    		"recipeSlug": fields.RecipeSlug,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetRecipe routes the request to either CN or Global logic based on input.
func bridgeGetRecipe(ctx context.Context, fields *GetRecipeFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetRecipe != nil {
			return CNHookGetRecipe(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetRecipe(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetRecipeModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetRecipe
/* GetRecipe Returns a single recipe by slug for the specified artisan. */
// Path: /d3/data/artisan/{artisanSlug}/recipe/{recipeSlug}
var GetRecipe = bridgeGetRecipe

