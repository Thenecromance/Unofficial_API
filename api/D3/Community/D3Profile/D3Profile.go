package D3_D3Profile

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: GetApiAccount
// ==============================================================================================

type GetApiAccountFields struct {
	Account string `uri:"account" binding:"required"` // The BattleTag for the account to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetApiAccount allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetApiAccount func(context.Context, *GetApiAccountFields) (any, error)

// StringGetApiAccount executes the request and returns the raw response body string.
func StringGetApiAccount(ctx context.Context, fields *GetApiAccountFields) (string, error) {
	if fields == nil {
		fields = &GetApiAccountFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetApiAccount")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/profile/{account}/",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"account": fields.Account,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetApiAccount routes the request to either CN or Global logic based on input.
func bridgeGetApiAccount(ctx context.Context, fields *GetApiAccountFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetApiAccount != nil {
			return CNHookGetApiAccount(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetApiAccount(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetApiAccountModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetApiAccount
/* GetApiAccount Returns the specified account profile. */
// Path: /d3/profile/{account}/
var GetApiAccount = bridgeGetApiAccount


// ==============================================================================================
// API: GetApiHero
// ==============================================================================================

type GetApiHeroFields struct {
	Account string `uri:"account" binding:"required"` // The BattleTag for the account to retrieve.
		HeroId string `uri:"heroId" binding:"required"` // The ID of the hero to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetApiHero allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetApiHero func(context.Context, *GetApiHeroFields) (any, error)

// StringGetApiHero executes the request and returns the raw response body string.
func StringGetApiHero(ctx context.Context, fields *GetApiHeroFields) (string, error) {
	if fields == nil {
		fields = &GetApiHeroFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetApiHero")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/profile/{account}/hero/{heroId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"account": fields.Account,
    		"heroId": fields.HeroId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetApiHero routes the request to either CN or Global logic based on input.
func bridgeGetApiHero(ctx context.Context, fields *GetApiHeroFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetApiHero != nil {
			return CNHookGetApiHero(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetApiHero(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetApiHeroModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetApiHero
/* GetApiHero Returns a single hero. */
// Path: /d3/profile/{account}/hero/{heroId}
var GetApiHero = bridgeGetApiHero


// ==============================================================================================
// API: GetApiDetailedHeroItems
// ==============================================================================================

type GetApiDetailedHeroItemsFields struct {
	Account string `uri:"account" binding:"required"` // The BattleTag for the account to retrieve.
		HeroId string `uri:"heroId" binding:"required"` // The ID of the hero to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetApiDetailedHeroItems allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetApiDetailedHeroItems func(context.Context, *GetApiDetailedHeroItemsFields) (any, error)

// StringGetApiDetailedHeroItems executes the request and returns the raw response body string.
func StringGetApiDetailedHeroItems(ctx context.Context, fields *GetApiDetailedHeroItemsFields) (string, error) {
	if fields == nil {
		fields = &GetApiDetailedHeroItemsFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetApiDetailedHeroItems")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/profile/{account}/hero/{heroId}/items",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"account": fields.Account,
    		"heroId": fields.HeroId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetApiDetailedHeroItems routes the request to either CN or Global logic based on input.
func bridgeGetApiDetailedHeroItems(ctx context.Context, fields *GetApiDetailedHeroItemsFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetApiDetailedHeroItems != nil {
			return CNHookGetApiDetailedHeroItems(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetApiDetailedHeroItems(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetApiDetailedHeroItemsModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetApiDetailedHeroItems
/* GetApiDetailedHeroItems Returns a list of items for the specified hero. */
// Path: /d3/profile/{account}/hero/{heroId}/items
var GetApiDetailedHeroItems = bridgeGetApiDetailedHeroItems


// ==============================================================================================
// API: GetApiDetailedFollowerItems
// ==============================================================================================

type GetApiDetailedFollowerItemsFields struct {
	Account string `uri:"account" binding:"required"` // The BattleTag for the account to retrieve.
		HeroId string `uri:"heroId" binding:"required"` // The ID of the hero to retrieve.
		Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookGetApiDetailedFollowerItems allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookGetApiDetailedFollowerItems func(context.Context, *GetApiDetailedFollowerItemsFields) (any, error)

// StringGetApiDetailedFollowerItems executes the request and returns the raw response body string.
func StringGetApiDetailedFollowerItems(ctx context.Context, fields *GetApiDetailedFollowerItemsFields) (string, error) {
	if fields == nil {
		fields = &GetApiDetailedFollowerItemsFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("GetApiDetailedFollowerItems")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/d3/profile/{account}/hero/{heroId}/follower-items",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"account": fields.Account,
    		"heroId": fields.HeroId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeGetApiDetailedFollowerItems routes the request to either CN or Global logic based on input.
func bridgeGetApiDetailedFollowerItems(ctx context.Context, fields *GetApiDetailedFollowerItemsFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookGetApiDetailedFollowerItems != nil {
			return CNHookGetApiDetailedFollowerItems(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringGetApiDetailedFollowerItems(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &GetApiDetailedFollowerItemsModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// GetApiDetailedFollowerItems
/* GetApiDetailedFollowerItems Returns a list of items for the specified hero's followers. */
// Path: /d3/profile/{account}/hero/{heroId}/follower-items
var GetApiDetailedFollowerItems = bridgeGetApiDetailedFollowerItems

