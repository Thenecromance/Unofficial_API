package wowClassic_AuctionHouse

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: AuctionHouseIndex
// ==============================================================================================

type AuctionHouseIndexFields struct {
	ConnectedRealmId int `uri:"connectedRealmId" binding:"required"` // The ID of the connected realm.
		Namespace string `form:"namespace,default=dynamic-classic-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAuctionHouseIndex allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAuctionHouseIndex func(context.Context, *AuctionHouseIndexFields) (any, error)

// StringAuctionHouseIndex executes the request and returns the raw response body string.
func StringAuctionHouseIndex(ctx context.Context, fields *AuctionHouseIndexFields) (string, error) {
	if fields == nil {
		fields = &AuctionHouseIndexFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AuctionHouseIndex")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/connected-realm/{connectedRealmId}/auctions/index",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.ConnectedRealmId == 0 {
		fields.ConnectedRealmId = 4372
	}
	
	if fields.Namespace == "" {
		fields.Namespace = "dynamic-classic-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"connectedRealmId": fields.ConnectedRealmId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "dynamic-classic-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAuctionHouseIndex routes the request to either CN or Global logic based on input.
func bridgeAuctionHouseIndex(ctx context.Context, fields *AuctionHouseIndexFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAuctionHouseIndex != nil {
			return CNHookAuctionHouseIndex(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAuctionHouseIndex(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AuctionHouseIndexModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AuctionHouseIndex
/* AuctionHouseIndex Returns an index of auction houses for a connected realm.<br/><br/>See the <strong>Connected Realm API</strong> for information about retrieving a list of connected realm IDs. */
// Path: /data/wow/connected-realm/{connectedRealmId}/auctions/index
var AuctionHouseIndex = bridgeAuctionHouseIndex


// ==============================================================================================
// API: Auctions
// ==============================================================================================

type AuctionsFields struct {
	ConnectedRealmId int `uri:"connectedRealmId" binding:"required"` // The ID of the connected realm.
		AuctionHouseId int `uri:"auctionHouseId" binding:"required"` // The ID of the auction house.
		Namespace string `form:"namespace,default=dynamic-classic-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAuctions allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAuctions func(context.Context, *AuctionsFields) (any, error)

// StringAuctions executes the request and returns the raw response body string.
func StringAuctions(ctx context.Context, fields *AuctionsFields) (string, error) {
	if fields == nil {
		fields = &AuctionsFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("Auctions")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/connected-realm/{connectedRealmId}/auctions/{auctionHouseId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.ConnectedRealmId == 0 {
		fields.ConnectedRealmId = 4372
	}
	
	if fields.AuctionHouseId == 0 {
		fields.AuctionHouseId = 2
	}
	
	if fields.Namespace == "" {
		fields.Namespace = "dynamic-classic-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"connectedRealmId": fields.ConnectedRealmId,
    		"auctionHouseId": fields.AuctionHouseId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "dynamic-classic-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAuctions routes the request to either CN or Global logic based on input.
func bridgeAuctions(ctx context.Context, fields *AuctionsFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAuctions != nil {
			return CNHookAuctions(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAuctions(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AuctionsModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// Auctions
/* Auctions Returns all active auctions for a specific auction house on a connected realm.<br/><br/>See the <strong>Connected Realm API</strong> for information about retrieving a list of connected realm IDs.<br/><br/>Auction house data updates at a set interval. The value was initially set at 1 hour; however, it might change over time without notice.<br/><br/>Depending on the number of active auctions on the specified connected realm, the response from this endpoint may be rather large, sometimes exceeding 10 MB. */
// Path: /data/wow/connected-realm/{connectedRealmId}/auctions/{auctionHouseId}
var Auctions = bridgeAuctions

