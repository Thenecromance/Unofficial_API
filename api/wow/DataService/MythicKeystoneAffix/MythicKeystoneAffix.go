package wow_MythicKeystoneAffix

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: MythicKeystoneAffixesIndex
// ==============================================================================================

type MythicKeystoneAffixesIndexFields struct {
	Namespace string `form:"namespace,default=static-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookMythicKeystoneAffixesIndex allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookMythicKeystoneAffixesIndex func(context.Context, *MythicKeystoneAffixesIndexFields) (any, error)

// StringMythicKeystoneAffixesIndex executes the request and returns the raw response body string.
func StringMythicKeystoneAffixesIndex(ctx context.Context, fields *MythicKeystoneAffixesIndexFields) (string, error) {
	if fields == nil {
		fields = &MythicKeystoneAffixesIndexFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("MythicKeystoneAffixesIndex")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/keystone-affix/index",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "static-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "static-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeMythicKeystoneAffixesIndex routes the request to either CN or Global logic based on input.
func bridgeMythicKeystoneAffixesIndex(ctx context.Context, fields *MythicKeystoneAffixesIndexFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookMythicKeystoneAffixesIndex != nil {
			return CNHookMythicKeystoneAffixesIndex(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringMythicKeystoneAffixesIndex(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &MythicKeystoneAffixesIndexModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// MythicKeystoneAffixesIndex
/* MythicKeystoneAffixesIndex Returns an index of mythic keystone affixes. */
// Path: /data/wow/keystone-affix/index
var MythicKeystoneAffixesIndex = bridgeMythicKeystoneAffixesIndex


// ==============================================================================================
// API: MythicKeystoneAffix
// ==============================================================================================

type MythicKeystoneAffixFields struct {
	KeystoneAffixId int `uri:"keystoneAffixId" binding:"required"` // The ID of the mythic keystone affix.
		Namespace string `form:"namespace,default=static-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookMythicKeystoneAffix allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookMythicKeystoneAffix func(context.Context, *MythicKeystoneAffixFields) (any, error)

// StringMythicKeystoneAffix executes the request and returns the raw response body string.
func StringMythicKeystoneAffix(ctx context.Context, fields *MythicKeystoneAffixFields) (string, error) {
	if fields == nil {
		fields = &MythicKeystoneAffixFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("MythicKeystoneAffix")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/keystone-affix/{keystoneAffixId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.KeystoneAffixId == 0 {
		fields.KeystoneAffixId = 1
	}
	
	if fields.Namespace == "" {
		fields.Namespace = "static-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"keystoneAffixId": fields.KeystoneAffixId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "static-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeMythicKeystoneAffix routes the request to either CN or Global logic based on input.
func bridgeMythicKeystoneAffix(ctx context.Context, fields *MythicKeystoneAffixFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookMythicKeystoneAffix != nil {
			return CNHookMythicKeystoneAffix(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringMythicKeystoneAffix(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &MythicKeystoneAffixModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// MythicKeystoneAffix
/* MythicKeystoneAffix Returns a mythic keystone affix by ID. */
// Path: /data/wow/keystone-affix/{keystoneAffixId}
var MythicKeystoneAffix = bridgeMythicKeystoneAffix


// ==============================================================================================
// API: MythicKeystoneAffixMedia
// ==============================================================================================

type MythicKeystoneAffixMediaFields struct {
	KeystoneAffixId int `uri:"keystoneAffixId" binding:"required"` // The ID of the mythic keystone affix.
		Namespace string `form:"namespace,default=static-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookMythicKeystoneAffixMedia allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookMythicKeystoneAffixMedia func(context.Context, *MythicKeystoneAffixMediaFields) (any, error)

// StringMythicKeystoneAffixMedia executes the request and returns the raw response body string.
func StringMythicKeystoneAffixMedia(ctx context.Context, fields *MythicKeystoneAffixMediaFields) (string, error) {
	if fields == nil {
		fields = &MythicKeystoneAffixMediaFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("MythicKeystoneAffixMedia")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/media/keystone-affix/{keystoneAffixId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.KeystoneAffixId == 0 {
		fields.KeystoneAffixId = 1
	}
	
	if fields.Namespace == "" {
		fields.Namespace = "static-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"keystoneAffixId": fields.KeystoneAffixId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "static-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeMythicKeystoneAffixMedia routes the request to either CN or Global logic based on input.
func bridgeMythicKeystoneAffixMedia(ctx context.Context, fields *MythicKeystoneAffixMediaFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookMythicKeystoneAffixMedia != nil {
			return CNHookMythicKeystoneAffixMedia(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringMythicKeystoneAffixMedia(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &MythicKeystoneAffixMediaModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// MythicKeystoneAffixMedia
/* MythicKeystoneAffixMedia Returns media for a mythic keystone affix by ID. */
// Path: /data/wow/media/keystone-affix/{keystoneAffixId}
var MythicKeystoneAffixMedia = bridgeMythicKeystoneAffixMedia

