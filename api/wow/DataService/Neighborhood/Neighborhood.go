package wow_Neighborhood

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: NeighborhoodMapIndex
// ==============================================================================================

type NeighborhoodMapIndexFields struct {
	Namespace string `form:"namespace,default=dynamic-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookNeighborhoodMapIndex allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookNeighborhoodMapIndex func(context.Context, *NeighborhoodMapIndexFields) (any, error)

// StringNeighborhoodMapIndex executes the request and returns the raw response body string.
func StringNeighborhoodMapIndex(ctx context.Context, fields *NeighborhoodMapIndexFields) (string, error) {
	if fields == nil {
		fields = &NeighborhoodMapIndexFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("NeighborhoodMapIndex")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/neighborhood-map/index",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "dynamic-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "dynamic-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeNeighborhoodMapIndex routes the request to either CN or Global logic based on input.
func bridgeNeighborhoodMapIndex(ctx context.Context, fields *NeighborhoodMapIndexFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookNeighborhoodMapIndex != nil {
			return CNHookNeighborhoodMapIndex(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringNeighborhoodMapIndex(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &NeighborhoodMapIndexModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// NeighborhoodMapIndex
/* NeighborhoodMapIndex Returns an index of neighborhood maps. */
// Path: /data/wow/neighborhood-map/index
var NeighborhoodMapIndex = bridgeNeighborhoodMapIndex


// ==============================================================================================
// API: NeighborhoodMap
// ==============================================================================================

type NeighborhoodMapFields struct {
	NeighborhoodMapId string `uri:"neighborhoodMapId" binding:"required"` // The ID of the neighborhood map.
		Namespace string `form:"namespace,default=dynamic-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookNeighborhoodMap allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookNeighborhoodMap func(context.Context, *NeighborhoodMapFields) (any, error)

// StringNeighborhoodMap executes the request and returns the raw response body string.
func StringNeighborhoodMap(ctx context.Context, fields *NeighborhoodMapFields) (string, error) {
	if fields == nil {
		fields = &NeighborhoodMapFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("NeighborhoodMap")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/neighborhood-map/{neighborhoodMapId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.NeighborhoodMapId == "" {
		fields.NeighborhoodMapId = "1"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "dynamic-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"neighborhoodMapId": fields.NeighborhoodMapId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "dynamic-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeNeighborhoodMap routes the request to either CN or Global logic based on input.
func bridgeNeighborhoodMap(ctx context.Context, fields *NeighborhoodMapFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookNeighborhoodMap != nil {
			return CNHookNeighborhoodMap(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringNeighborhoodMap(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &NeighborhoodMapModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// NeighborhoodMap
/* NeighborhoodMap Returns a neighborhood map by ID. */
// Path: /data/wow/neighborhood-map/{neighborhoodMapId}
var NeighborhoodMap = bridgeNeighborhoodMap


// ==============================================================================================
// API: Neighborhood
// ==============================================================================================

type NeighborhoodFields struct {
	NeighborhoodMapId string `uri:"neighborhoodMapId" binding:"required"` // The ID of the neighborhood map.
		NeighborhoodId string `uri:"neighborhoodId" binding:"required"` // The ID of the neighborhood.
		Namespace string `form:"namespace,default=dynamic-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookNeighborhood allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookNeighborhood func(context.Context, *NeighborhoodFields) (any, error)

// StringNeighborhood executes the request and returns the raw response body string.
func StringNeighborhood(ctx context.Context, fields *NeighborhoodFields) (string, error) {
	if fields == nil {
		fields = &NeighborhoodFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("Neighborhood")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/data/wow/neighborhood-map/{neighborhoodMapId}/neighborhood/{neighborhoodId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.NeighborhoodMapId == "" {
		fields.NeighborhoodMapId = "2"
	}
	
	
	if fields.NeighborhoodId == "" {
		fields.NeighborhoodId = "17902"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "dynamic-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"neighborhoodMapId": fields.NeighborhoodMapId,
    		"neighborhoodId": fields.NeighborhoodId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "dynamic-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeNeighborhood routes the request to either CN or Global logic based on input.
func bridgeNeighborhood(ctx context.Context, fields *NeighborhoodFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookNeighborhood != nil {
			return CNHookNeighborhood(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringNeighborhood(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &NeighborhoodModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// Neighborhood
/* Neighborhood Returns a neighborhood by ID. */
// Path: /data/wow/neighborhood-map/{neighborhoodMapId}/neighborhood/{neighborhoodId}
var Neighborhood = bridgeNeighborhood

