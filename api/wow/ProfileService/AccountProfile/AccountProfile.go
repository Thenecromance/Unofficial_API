package wow_AccountProfile

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: AccountProfileSummary
// ==============================================================================================

type AccountProfileSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountProfileSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountProfileSummary func(context.Context, *AccountProfileSummaryFields) (any, error)

// StringAccountProfileSummary executes the request and returns the raw response body string.
func StringAccountProfileSummary(ctx context.Context, fields *AccountProfileSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountProfileSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountProfileSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountProfileSummary routes the request to either CN or Global logic based on input.
func bridgeAccountProfileSummary(ctx context.Context, fields *AccountProfileSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountProfileSummary != nil {
			return CNHookAccountProfileSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountProfileSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountProfileSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountProfileSummary
/* AccountProfileSummary Returns a profile summary for an account.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow
var AccountProfileSummary = bridgeAccountProfileSummary


// ==============================================================================================
// API: ProtectedCharacterProfileSummary
// ==============================================================================================

type ProtectedCharacterProfileSummaryFields struct {
	RealmId int `uri:"realmId" binding:"required"` // The ID of the character's realm.
		CharacterId int `uri:"characterId" binding:"required"` // The ID of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookProtectedCharacterProfileSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookProtectedCharacterProfileSummary func(context.Context, *ProtectedCharacterProfileSummaryFields) (any, error)

// StringProtectedCharacterProfileSummary executes the request and returns the raw response body string.
func StringProtectedCharacterProfileSummary(ctx context.Context, fields *ProtectedCharacterProfileSummaryFields) (string, error) {
	if fields == nil {
		fields = &ProtectedCharacterProfileSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("ProtectedCharacterProfileSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/protected-character/{realmId}-{characterId}",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmId == 0 {
		fields.RealmId = 1
	}
	
	if fields.CharacterId == 0 {
		fields.CharacterId = 12345
	}
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmId": fields.RealmId,
    		"characterId": fields.CharacterId,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeProtectedCharacterProfileSummary routes the request to either CN or Global logic based on input.
func bridgeProtectedCharacterProfileSummary(ctx context.Context, fields *ProtectedCharacterProfileSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookProtectedCharacterProfileSummary != nil {
			return CNHookProtectedCharacterProfileSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringProtectedCharacterProfileSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &ProtectedCharacterProfileSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// ProtectedCharacterProfileSummary
/* ProtectedCharacterProfileSummary Returns a protected profile summary for a character.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/protected-character/{realmId}-{characterId}
var ProtectedCharacterProfileSummary = bridgeProtectedCharacterProfileSummary


// ==============================================================================================
// API: AccountCollectionsIndex
// ==============================================================================================

type AccountCollectionsIndexFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountCollectionsIndex allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountCollectionsIndex func(context.Context, *AccountCollectionsIndexFields) (any, error)

// StringAccountCollectionsIndex executes the request and returns the raw response body string.
func StringAccountCollectionsIndex(ctx context.Context, fields *AccountCollectionsIndexFields) (string, error) {
	if fields == nil {
		fields = &AccountCollectionsIndexFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountCollectionsIndex")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountCollectionsIndex routes the request to either CN or Global logic based on input.
func bridgeAccountCollectionsIndex(ctx context.Context, fields *AccountCollectionsIndexFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountCollectionsIndex != nil {
			return CNHookAccountCollectionsIndex(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountCollectionsIndex(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountCollectionsIndexModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountCollectionsIndex
/* AccountCollectionsIndex Returns an index of collection types for an account.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections
var AccountCollectionsIndex = bridgeAccountCollectionsIndex


// ==============================================================================================
// API: AccountDecorCollectionSummary
// ==============================================================================================

type AccountDecorCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountDecorCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountDecorCollectionSummary func(context.Context, *AccountDecorCollectionSummaryFields) (any, error)

// StringAccountDecorCollectionSummary executes the request and returns the raw response body string.
func StringAccountDecorCollectionSummary(ctx context.Context, fields *AccountDecorCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountDecorCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountDecorCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/decor",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountDecorCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountDecorCollectionSummary(ctx context.Context, fields *AccountDecorCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountDecorCollectionSummary != nil {
			return CNHookAccountDecorCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountDecorCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountDecorCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountDecorCollectionSummary
/* AccountDecorCollectionSummary Returns a summary of the housing decor an account has collected.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/decor
var AccountDecorCollectionSummary = bridgeAccountDecorCollectionSummary


// ==============================================================================================
// API: AccountHeirloomsCollectionSummary
// ==============================================================================================

type AccountHeirloomsCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountHeirloomsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountHeirloomsCollectionSummary func(context.Context, *AccountHeirloomsCollectionSummaryFields) (any, error)

// StringAccountHeirloomsCollectionSummary executes the request and returns the raw response body string.
func StringAccountHeirloomsCollectionSummary(ctx context.Context, fields *AccountHeirloomsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountHeirloomsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountHeirloomsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/heirlooms",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountHeirloomsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountHeirloomsCollectionSummary(ctx context.Context, fields *AccountHeirloomsCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountHeirloomsCollectionSummary != nil {
			return CNHookAccountHeirloomsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountHeirloomsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountHeirloomsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountHeirloomsCollectionSummary
/* AccountHeirloomsCollectionSummary Returns a summary of the heirlooms an account has obtained.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/heirlooms
var AccountHeirloomsCollectionSummary = bridgeAccountHeirloomsCollectionSummary


// ==============================================================================================
// API: AccountMountsCollectionSummary
// ==============================================================================================

type AccountMountsCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountMountsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountMountsCollectionSummary func(context.Context, *AccountMountsCollectionSummaryFields) (any, error)

// StringAccountMountsCollectionSummary executes the request and returns the raw response body string.
func StringAccountMountsCollectionSummary(ctx context.Context, fields *AccountMountsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountMountsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountMountsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/mounts",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountMountsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountMountsCollectionSummary(ctx context.Context, fields *AccountMountsCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountMountsCollectionSummary != nil {
			return CNHookAccountMountsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountMountsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountMountsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountMountsCollectionSummary
/* AccountMountsCollectionSummary Returns a summary of the mounts an account has obtained.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/mounts
var AccountMountsCollectionSummary = bridgeAccountMountsCollectionSummary


// ==============================================================================================
// API: AccountPetsCollectionSummary
// ==============================================================================================

type AccountPetsCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountPetsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountPetsCollectionSummary func(context.Context, *AccountPetsCollectionSummaryFields) (any, error)

// StringAccountPetsCollectionSummary executes the request and returns the raw response body string.
func StringAccountPetsCollectionSummary(ctx context.Context, fields *AccountPetsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountPetsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountPetsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/pets",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountPetsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountPetsCollectionSummary(ctx context.Context, fields *AccountPetsCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountPetsCollectionSummary != nil {
			return CNHookAccountPetsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountPetsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountPetsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountPetsCollectionSummary
/* AccountPetsCollectionSummary Returns a summary of the battle pets an account has obtained.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/pets
var AccountPetsCollectionSummary = bridgeAccountPetsCollectionSummary


// ==============================================================================================
// API: AccountToysCollectionSummary
// ==============================================================================================

type AccountToysCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountToysCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountToysCollectionSummary func(context.Context, *AccountToysCollectionSummaryFields) (any, error)

// StringAccountToysCollectionSummary executes the request and returns the raw response body string.
func StringAccountToysCollectionSummary(ctx context.Context, fields *AccountToysCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountToysCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountToysCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/toys",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountToysCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountToysCollectionSummary(ctx context.Context, fields *AccountToysCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountToysCollectionSummary != nil {
			return CNHookAccountToysCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountToysCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountToysCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountToysCollectionSummary
/* AccountToysCollectionSummary Returns a summary of the toys an account has obtained.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/toys
var AccountToysCollectionSummary = bridgeAccountToysCollectionSummary


// ==============================================================================================
// API: AccountTransmogCollectionSummary
// ==============================================================================================

type AccountTransmogCollectionSummaryFields struct {
	Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookAccountTransmogCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookAccountTransmogCollectionSummary func(context.Context, *AccountTransmogCollectionSummaryFields) (any, error)

// StringAccountTransmogCollectionSummary executes the request and returns the raw response body string.
func StringAccountTransmogCollectionSummary(ctx context.Context, fields *AccountTransmogCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &AccountTransmogCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("AccountTransmogCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/user/wow/collections/transmogs",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeAccountTransmogCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeAccountTransmogCollectionSummary(ctx context.Context, fields *AccountTransmogCollectionSummaryFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookAccountTransmogCollectionSummary != nil {
			return CNHookAccountTransmogCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringAccountTransmogCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &AccountTransmogCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// AccountTransmogCollectionSummary
/* AccountTransmogCollectionSummary Returns a summary of the transmog unlocks an account has obtained.<br/><br/>Because this endpoint provides data about the current logged-in user's World of Warcraft account, it requires an access token with the <strong>wow.profile</strong> scope acquired via the <a href="/documentation/guides/using-oauth/authorization-code-flow">Authorization Code Flow</a>. */
// Path: /profile/user/wow/collections/transmogs
var AccountTransmogCollectionSummary = bridgeAccountTransmogCollectionSummary

