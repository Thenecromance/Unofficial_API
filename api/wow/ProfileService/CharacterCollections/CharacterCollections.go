package wow_CharacterCollections

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	

	
	    "strings"
    

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: CharacterCollectionsIndex
// ==============================================================================================

type CharacterCollectionsIndexFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterCollectionsIndex allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterCollectionsIndex func(context.Context, *CharacterCollectionsIndexFields) (any, error)

// StringCharacterCollectionsIndex executes the request and returns the raw response body string.
func StringCharacterCollectionsIndex(ctx context.Context, fields *CharacterCollectionsIndexFields) (string, error) {
	if fields == nil {
		fields = &CharacterCollectionsIndexFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterCollectionsIndex")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterCollectionsIndex routes the request to either CN or Global logic based on input.
func bridgeCharacterCollectionsIndex(ctx context.Context, fields *CharacterCollectionsIndexFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterCollectionsIndex != nil {
			return CNHookCharacterCollectionsIndex(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterCollectionsIndex(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterCollectionsIndexModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterCollectionsIndex
/* CharacterCollectionsIndex Returns an index of collection types for a character. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections
var CharacterCollectionsIndex = bridgeCharacterCollectionsIndex


// ==============================================================================================
// API: CharacterDecorCollectionSummary
// ==============================================================================================

type CharacterDecorCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterDecorCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterDecorCollectionSummary func(context.Context, *CharacterDecorCollectionSummaryFields) (any, error)

// StringCharacterDecorCollectionSummary executes the request and returns the raw response body string.
func StringCharacterDecorCollectionSummary(ctx context.Context, fields *CharacterDecorCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterDecorCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterDecorCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/decor",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterDecorCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterDecorCollectionSummary(ctx context.Context, fields *CharacterDecorCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterDecorCollectionSummary != nil {
			return CNHookCharacterDecorCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterDecorCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterDecorCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterDecorCollectionSummary
/* CharacterDecorCollectionSummary Returns a summary of the housing decor collection a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/decor
var CharacterDecorCollectionSummary = bridgeCharacterDecorCollectionSummary


// ==============================================================================================
// API: CharacterHeirloomsCollectionSummary
// ==============================================================================================

type CharacterHeirloomsCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterHeirloomsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterHeirloomsCollectionSummary func(context.Context, *CharacterHeirloomsCollectionSummaryFields) (any, error)

// StringCharacterHeirloomsCollectionSummary executes the request and returns the raw response body string.
func StringCharacterHeirloomsCollectionSummary(ctx context.Context, fields *CharacterHeirloomsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterHeirloomsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterHeirloomsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/heirlooms",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterHeirloomsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterHeirloomsCollectionSummary(ctx context.Context, fields *CharacterHeirloomsCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterHeirloomsCollectionSummary != nil {
			return CNHookCharacterHeirloomsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterHeirloomsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterHeirloomsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterHeirloomsCollectionSummary
/* CharacterHeirloomsCollectionSummary Returns a summary of the heirlooms a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/heirlooms
var CharacterHeirloomsCollectionSummary = bridgeCharacterHeirloomsCollectionSummary


// ==============================================================================================
// API: CharacterMountsCollectionSummary
// ==============================================================================================

type CharacterMountsCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterMountsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterMountsCollectionSummary func(context.Context, *CharacterMountsCollectionSummaryFields) (any, error)

// StringCharacterMountsCollectionSummary executes the request and returns the raw response body string.
func StringCharacterMountsCollectionSummary(ctx context.Context, fields *CharacterMountsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterMountsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterMountsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/mounts",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterMountsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterMountsCollectionSummary(ctx context.Context, fields *CharacterMountsCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterMountsCollectionSummary != nil {
			return CNHookCharacterMountsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterMountsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterMountsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterMountsCollectionSummary
/* CharacterMountsCollectionSummary Returns a summary of the mounts a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/mounts
var CharacterMountsCollectionSummary = bridgeCharacterMountsCollectionSummary


// ==============================================================================================
// API: CharacterPetsCollectionSummary
// ==============================================================================================

type CharacterPetsCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterPetsCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterPetsCollectionSummary func(context.Context, *CharacterPetsCollectionSummaryFields) (any, error)

// StringCharacterPetsCollectionSummary executes the request and returns the raw response body string.
func StringCharacterPetsCollectionSummary(ctx context.Context, fields *CharacterPetsCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterPetsCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterPetsCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/pets",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterPetsCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterPetsCollectionSummary(ctx context.Context, fields *CharacterPetsCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterPetsCollectionSummary != nil {
			return CNHookCharacterPetsCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterPetsCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterPetsCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterPetsCollectionSummary
/* CharacterPetsCollectionSummary Returns a summary of the battle pets a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/pets
var CharacterPetsCollectionSummary = bridgeCharacterPetsCollectionSummary


// ==============================================================================================
// API: CharacterToysCollectionSummary
// ==============================================================================================

type CharacterToysCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterToysCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterToysCollectionSummary func(context.Context, *CharacterToysCollectionSummaryFields) (any, error)

// StringCharacterToysCollectionSummary executes the request and returns the raw response body string.
func StringCharacterToysCollectionSummary(ctx context.Context, fields *CharacterToysCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterToysCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterToysCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/toys",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterToysCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterToysCollectionSummary(ctx context.Context, fields *CharacterToysCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterToysCollectionSummary != nil {
			return CNHookCharacterToysCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterToysCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterToysCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterToysCollectionSummary
/* CharacterToysCollectionSummary Returns a summary of the toys a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/toys
var CharacterToysCollectionSummary = bridgeCharacterToysCollectionSummary


// ==============================================================================================
// API: CharacterTransmogCollectionSummary
// ==============================================================================================

type CharacterTransmogCollectionSummaryFields struct {
	RealmSlug string `uri:"realmSlug" binding:"required"` // The slug of the realm.
		CharacterName string `uri:"characterName" binding:"required"` // The lowercase name of the character.
		Namespace string `form:"namespace,default=profile-us"` // The namespace to use to locate this document.
	Locale string `form:"locale,default=en_US"` // The locale to reflect in localized data.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCharacterTransmogCollectionSummary allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCharacterTransmogCollectionSummary func(context.Context, *CharacterTransmogCollectionSummaryFields) (any, error)

// StringCharacterTransmogCollectionSummary executes the request and returns the raw response body string.
func StringCharacterTransmogCollectionSummary(ctx context.Context, fields *CharacterTransmogCollectionSummaryFields) (string, error) {
	if fields == nil {
		fields = &CharacterTransmogCollectionSummaryFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CharacterTransmogCollectionSummary")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/profile/wow/character/{realmSlug}/{characterName}/collections/transmogs",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.RealmSlug == "" {
		fields.RealmSlug = "tichondrius"
	}
	
	
	if fields.CharacterName == "" {
		fields.CharacterName = "charactername"
	}
	
	
	if fields.Namespace == "" {
		fields.Namespace = "profile-us"
	}
	
	
	if fields.Locale == "" {
		fields.Locale = "en_US"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"realmSlug": fields.RealmSlug,
    		"characterName": fields.CharacterName,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("namespace") {
		q.Add("namespace", "profile-us")
	}
    
    
	if !q.Has("locale") {
		q.Add("locale", "en_US")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCharacterTransmogCollectionSummary routes the request to either CN or Global logic based on input.
func bridgeCharacterTransmogCollectionSummary(ctx context.Context, fields *CharacterTransmogCollectionSummaryFields) (any, error) {
    
	if strings.Contains(fields.Namespace, "-cn") {
		if fields.CN == nil {
			fields.CN = &utils.CNRequestMethod{
				
				Name:      fields.CharacterName,
				
				RealmSlug: fields.RealmSlug,
			}
		}
	}
	

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCharacterTransmogCollectionSummary != nil {
			return CNHookCharacterTransmogCollectionSummary(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCharacterTransmogCollectionSummary(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CharacterTransmogCollectionSummaryModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CharacterTransmogCollectionSummary
/* CharacterTransmogCollectionSummary Returns a summary of the transmog unlocks a character has obtained. */
// Path: /profile/wow/character/{realmSlug}/{characterName}/collections/transmogs
var CharacterTransmogCollectionSummary = bridgeCharacterTransmogCollectionSummary

