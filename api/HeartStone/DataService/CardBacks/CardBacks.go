package HeartStone_CardBacks

// AUTOMATICALLY GENERATED FILE, DO NOT EDIT
// This file is generated by tools/updater
// Author: @Thenecromance

import (
	"context"
	"encoding/json"
	
	    "strconv"
	

	

	"io"
	"net/http"

	"Unofficial_API/ApiError"
	"Unofficial_API/api/Authentication"
	"Unofficial_API/global"
	"Unofficial_API/utils"


	"github.com/jtacoma/uritemplates"

)


// ==============================================================================================
// API: CardBackSearch
// ==============================================================================================

type CardBackSearchFields struct {
	Locale string `form:"locale"` // The locale to reflect in localized data. If you do not supply a value, all translations are returned.
	CardBackCategory string `form:"cardBackCategory"` // A category of the card back. The category must match a valid category.
	TextFilter string `form:"textFilter"` // A text string used to filter card backs. You must include a locale along with the textFilter parameter.
	Sort string `form:"sort,default=dateAdded:desc"` // The field used to sort the results. Valid value include name:asc, name:desc, dateAdded:asc, and dateAdded:desc. Results are sorted by date (desc) by default.
	Page int `form:"page"` // A page number.
	PageSize int `form:"pageSize"` // The number of results to choose per page. A value will be selected automatically if you do not supply a pageSize or if the pageSize is higher than the maximum allowed.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookCardBackSearch allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookCardBackSearch func(context.Context, *CardBackSearchFields) (any, error)

// StringCardBackSearch executes the request and returns the raw response body string.
func StringCardBackSearch(ctx context.Context, fields *CardBackSearchFields) (string, error) {
	if fields == nil {
		fields = &CardBackSearchFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("CardBackSearch")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/hearthstone/cardbacks",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Sort == "" {
		fields.Sort = "dateAdded:desc"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	req.URL.Path = fields.Path
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "<no value>")
	}
    
    
	if !q.Has("cardBackCategory") {
		q.Add("cardBackCategory", "<no value>")
	}
    
    
	if !q.Has("textFilter") {
		q.Add("textFilter", "<no value>")
	}
    
    
	if !q.Has("sort") {
		q.Add("sort", "dateAdded:desc")
	}
    
    
    	if !q.Has("page") {
    		q.Add("page", strconv.Itoa(fields.Page))
    	}
    
    
    	if !q.Has("pageSize") {
    		q.Add("pageSize", strconv.Itoa(fields.PageSize))
    	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeCardBackSearch routes the request to either CN or Global logic based on input.
func bridgeCardBackSearch(ctx context.Context, fields *CardBackSearchFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookCardBackSearch != nil {
			return CNHookCardBackSearch(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringCardBackSearch(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &CardBackSearchModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// CardBackSearch
/* CardBackSearch Returns an up-to-date list of all card backs matching the search criteria. For more information about the search parameters, see the <a href='/documentation/hearthstone/guides/card-backs'>Card Backs Guide</a>. */
// Path: /hearthstone/cardbacks
var CardBackSearch = bridgeCardBackSearch


// ==============================================================================================
// API: Fetchonecardback
// ==============================================================================================

type FetchonecardbackFields struct {
	Idorslug string `uri:"idorslug" binding:"required"` // An ID or slug that uniquely identifies a card back. You can discover these values by using the GET /hearthstone/cardbacks endpoint
		Locale string `form:"locale"` // The locale to reflect in localized data. If you do not supply a value, all translations are returned.

	// Extra fields for internal logic
	ExtraFields map[any]any
	CN          *utils.CNRequestMethod
	utils.RequestMethod
}

// CNHookFetchonecardback allows injecting a custom implementation for the CN endpoint.
// Implement this in a separate file (e.g. init()) to handle CN logic without modifying generated code.
var CNHookFetchonecardback func(context.Context, *FetchonecardbackFields) (any, error)

// StringFetchonecardback executes the request and returns the raw response body string.
func StringFetchonecardback(ctx context.Context, fields *FetchonecardbackFields) (string, error) {
	if fields == nil {
		fields = &FetchonecardbackFields{}
	}

	// 1. Initialize Request Metadata
	fields.RequestMethod = utils.GetApiConfig("Fetchonecardback")
	if fields.RequestMethod.Path == "" {
		fields.RequestMethod = utils.RequestMethod{
			Methods:  "GET",
			Path:     "/hearthstone/cardbacks/:idorslug",
			CnRegion: false,
		}
	}

	// 2. Apply Default Values (if needed for client-side logic)
	// Note: Usually struct tags handle server-side binding,
	// but here we might need manual checks if 0/"" are invalid for the request.
	
	if fields.Idorslug == "" {
		fields.Idorslug = "155-pizza-stone"
	}
	
	

	// 3. Create HTTP Request
	req, err := http.NewRequestWithContext(
		ctx,
		fields.Methods,
		global.PortalURL, // Base URL will be joined with Path below
		nil,
	)
	if err != nil {
		return "", err
	}

	// Add Auth Header
	req.Header.Add("Authorization", "Bearer "+Authentication.GetToken())

	// 4. Resolve Path (Handle URI Bindings)
	{
	
    	tpl, err := uritemplates.Parse(fields.Path)
    	if err != nil {
    		return "", err
    	}

    	pathValues := map[string]interface{}{
    		"idorslug": fields.Idorslug,
    		
    	}

    	expandedPath, err := tpl.Expand(pathValues)
    	if err != nil {
    		return "", err
    	}
    	req.URL.Path = expandedPath
    	
	}

	// 5. Build Query Strings
{
	q := req.URL.Query()


	for key, value := range fields.ExtraFields {
		q.Add(key.(string), value.(string))
	}

	
    
	if !q.Has("locale") {
		q.Add("locale", "<no value>")
	}
    


	req.URL.RawQuery = q.Encode()
}

	// 6. Execute Request
	cli := http.Client{}
	resp, err := cli.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()

	responseBytes, err := io.ReadAll(resp.Body)
	if err != nil {
		return "", err
	}

	return string(responseBytes), nil
}

// bridgeFetchonecardback routes the request to either CN or Global logic based on input.
func bridgeFetchonecardback(ctx context.Context, fields *FetchonecardbackFields) (any, error) {
    

	// 1. If CN specific parameters are present, use CN logic
	if fields.CN != nil {
        // Design Scheme: Check if a custom CN handler is registered at runtime.
		// This allows extension without modifying the template generator.
		if CNHookFetchonecardback != nil {
			return CNHookFetchonecardback(ctx, fields)
		}

		return nil, ApiError.ErrorNotSupported
	}

	// 2. Default: Use Global/Standard logic
	objString, err := StringFetchonecardback(ctx, fields)
	if err != nil {
		return nil, err
	}

	resp := &FetchonecardbackModel{}
	if err = json.Unmarshal([]byte(objString), &resp); err != nil {
		return nil, err
	}

	return resp, nil
}

// Fetchonecardback
/* Fetchonecardback Returns a specific card back by using card back ID or slug. */
// Path: /hearthstone/cardbacks/:idorslug
var Fetchonecardback = bridgeFetchonecardback

